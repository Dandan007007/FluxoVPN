name: fluxo_vpn
description: App FLUXO VPN - Conexão segura com VPS MANIA
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^11.0.1
  v2ray: ^0.1.2  # ✅ Biblioteca válida e atualizada!

flutter:
  uses-material-design: true
